/**
 * Q16. Write a program to display the number of characters for a string input.
 *      The string should be entered as a command line argument using (String [ ] args).
 */

package com.revature.question16;

import java.util.Scanner;

public class Question16 {
    private int numberOfCharacters;

    public Question16() {
        this.numberOfCharacters = numberOfCharacters;
        Scanner scanner = new Scanner(System.in);
        //numberOfCharacters = getNumberOfCharacters(scanner.next());
    }
    public int getNumberOfCharacters(String[] aString){

        //remove all white spaces
        String[] temp = aString;

        return 0;
    }
}
