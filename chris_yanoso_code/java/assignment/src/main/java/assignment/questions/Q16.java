package assignment.questions;

public class Q16 {
    public int charactersInString(String string) {
        //takes scanner input and returns the length of string (including spaces)
        return string.length();
    }
}